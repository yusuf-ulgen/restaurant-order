# ==============================================================================
# PRODUCTION SLOT GREEN DOCKER COMPOSE OVERRIDE (compose.prod.green.yml)
# ==============================================================================
# Usage: docker compose -f compose.yml -f compose.prod.green.yml up -d
# Green slot runs on port 5002 for zero-conflict concurrent blue-green deployment.
# Production DB and Redis are managed external services; no container ports exposed.
# ==============================================================================

services:
  api:
    container_name: restaurant-order-api-green
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
      DEPLOYMENT_COLOR: green
      PORT: 5000
    ports:
      - "${API_PORT_GREEN:-5002}:5000"

  worker:
    container_name: restaurant-order-worker-green
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      DEPLOYMENT_COLOR: green
      ACTIVE_DEPLOYMENT_SLOT: ${ACTIVE_DEPLOYMENT_SLOT:-blue}
