# ==============================================================================
# ACTIVE UPSTREAM CONFIGURATION
# Slot: blue | Routing via Docker container DNS on internal port
# ==============================================================================
upstream api_backend {
    server restaurant-order-api-blue:5000 max_fails=3 fail_timeout=10s;
    keepalive 32;
}

upstream customer_web_backend {
    server restaurant-order-customer-web-blue:8080 max_fails=3 fail_timeout=10s;
    keepalive 32;
}

upstream operations_web_backend {
    server restaurant-order-operations-web-blue:8080 max_fails=3 fail_timeout=10s;
    keepalive 32;
}

upstream admin_web_backend {
    server restaurant-order-admin-web-blue:8080 max_fails=3 fail_timeout=10s;
    keepalive 32;
}
